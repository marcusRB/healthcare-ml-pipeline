# 🩺 Healthcare Prediction API (Local Demo)

A lightweight FastAPI app demonstrating a machine learning workflow for predicting diabetes risk — **fully local, no external dependencies**.

---

## 🚀 Run Locally

```bash
# Install dependencies
pip install -r requirements.txt

# Start the API
uvicorn app.main:app --reload
